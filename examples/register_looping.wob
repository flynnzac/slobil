' This example demostrates creating variable argument instructions using
' register-looping

set /append
    (
      ' append registry prefix data1 data2 ... dataN

      set /cur [ to-register [ string-append prefix "1" . ] . ] .

      while ( exist cur . )
            (
              
          

