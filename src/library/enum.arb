' enum /levels REGISTRY
' REGISTRY gives a list of registers to create the enumeration at.  The values are at /#1, /#2, and so on.
set /enum
    (
      set /loc /#1 .
      set /val 1 .
      while ( exist loc levels . )
	    (
	      up set levels:loc val .
	      set /val [ add val 1 . ] .
	      set /loc [ next loc . ] .
	    ) .
    ) .
