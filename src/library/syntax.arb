' syntax expects /arg to be set to the argument and /check to be set to the test.
' Ex.
' syntax /arg x /check is-integer .
' You could potentially do something like:

set /syntax
    (
      if [ check arg . ] True
         {
           error "Argument is not in the domain of the instruction." .
         } .
    ) .



